IDEAS:
-Download background image from gog sb and use it as gamecard backbround

TO DO MAYBE : 
- SQL LITE
- Pour ne pas perdre l'avantage du CSV (la visibilité), tu pourras utiliser un outil gratuit comme "DB Browser for SQLite". C'est un logiciel qui te permet de voir ta base SQL exactement comme un tableau Excel, de modifier les cases à la main et de faire des recherches. Ça t'enlèvera la peur de "ne plus voir tes données".

TO DO :

- ViGaVaukt_UI is also getting quite big, is there a way to split it intelligently ?
 - ViGaVault_Scan.py is also getting quite big, is there a way to split it intelligently ?
- in the merge selection windows the path is never displaied instead I see \..

- In the edit windows, I wouldlike to have+/- the same layout for the 3 media zones (image, video and trailer, video will not have an image of course, but i want the same layout for the buttons and field.
So instead of URL it would be PATH for image and video,  use a textbox (not editable) with the the image/video full path in it, instead of copy, pu a select button, and instead of "play in browser" put "view full size" for the image and "play video" for the video. I hape i was clear enough :-)

- make the text selectable in the statistic windows

- Clean up genre, we have duplicates in genre because of the different way the same genre is named, example, we currently have RPG, Role Playing Game and Role playing game (RPG). We need to normalize this and we need to do it at the scan level, when that metadata is acquired. But the normalization should not be limited ti punctation or capital letters, there are a lot of acronyms to consider, like the example.
Could you ceate a normalisation scheme for the genres.

- when searching on igdb, isn't there a way to choose between the igdb candidates, the original game instaed of the dlc, or a collector version. i know that on the igdb site, I can see some tags, like "main game" or "standalone expansion" or "port"... can we use these to identify the main game ? Is it relevant for our case. Because I noticed that in many ces it took the most recent updated version of the game instead of the game itself.

- Fix the "Unknown" platform issue.

- Use folder structure from the /local folder to add a tag 

- To select a gamecard I noticed that some "zones" in the gamecard will not react to the click and other would. If you click on the image it always work, but if you click on the text zone it almost never work but somtimes it does. and if you click under the text it does.

- In the media manager , despite all modification to speed up db acceesses, when clicking on the scan button with all 3 checkboxes selected, it can take up to 15 sec to show a result, this is way too long. How can we fix this ?

 - In Settings/Local Sources/media download, movbe the Download video checkmark on the same line as video path at the for right. remove the space between the text"video path" and the text box for the path itself. Do the same for Images by adding the download images checkbox and adapt the program to take in account the checkbox to download or not the images.

 - If trying to go in the media manager, manual scan or settings while a FULL Scan is ongoing should give a message popup saying, "please wait for the scan to finish" to avoid data corruption/issues.


 -Platforms Manager !
Future structure : 
ViGaVault/
├── ViGaVault_UI.py             # (Existant - Ton interface)
├── ViGaVault_Scan.py           # (Existant - Ton LibraryManager et CSV)
├── ViGaVault_SCRAPPERS.py      # L'orchestrateur (À créer)
└── connectors/                 
    └── steam_cloud.py          # Le spécialiste Steam (À créer)
Voici les informations techniques que tu dois donner à Gemini pour qu'il code le connecteur :
Source de données : API Store Front de Steam.
Endpoint : https://store.steampowered.com/api/appdetails?appids={APPID}&l=french
Données à extraire (Metadata) :
---------------------------------------------------------------------------------------------------------
Type,		Champ Steam API,									Action ViGaVault
---------------------------------------------------------------------------------------------------------
Metadata,	"name, developers, publishers, release_date",		Injection CSV via LibraryManager
Texte,		"about_the_game, short_description",				Récupéré dans la langue de l'UI
Genres,		genres,												Passage par ta fonction normalize_genre
Images,		"header_image (Cover), 								background",Téléchargement dans ./images/
Vidéos,		movies -> mp4 -> max,								Téléchargement .mp4 si lien direct
Vidéos,		YouTube URL,										Copie simple du lien dans le CSV
---------------------------------------------------------------------------------------------------------
Si un lien .mp4 ou .webm existe (ex: mp4['max']) -> Téléchargement direct dans ./videos/.
Si seul un lien YouTube est présent -> Copie de l'URL dans le CSV uniquement.

3. Protocole de Connexion (Le Flux)

Récupération de la liste : On utilise l'URL userdata de Steam (nécessite les cookies de session de l'utilisateur) pour obtenir tous les AppIDs possédés. 
Boucle d'enrichissement : Pour chaque AppID, on appelle l'API appdetails pour obtenir le "gros" JSON.
Injection : On passe ce dictionnaire à LibraryManager.add_game_to_db pour qu'il s'occupe de l'écriture dans VGVDB.csv et du rangement des fichiers.

4. Le Guide pour Gemini dans VS Code
Copie ce bloc et colle-le dans ton chat VS Code une fois les fichiers ouverts :

"@workspace, nous allons implémenter le connecteur Cloud pour Steam.
Fichiers de référence : 
1. backend_steam_network.py pour voir comment ils gèrent les requêtes Web.
2. ViGaVault_Scan.py pour utiliser mon LibraryManager (méthode add_game_to_db).

Travail à réaliser :
Crée connectors/steam_cloud.py. Ce module doit contenir une classe capable de :
Récupérer la liste des AppIDs possédés via l'URL Steam UserData.
Pour chaque jeu, interroger l'API Store Front (appdetails).
Extraire TOUTES les métadonnées (Dev, Éditeur, Date, Résumé, Genres).
Médias : Télécharger les images (header) et les vidéos si un lien direct (.mp4/.webm) est disponible. Si c'est du YouTube, stocker l'URL sans télécharger.
Faire en sorte que le résultat final soit injecté proprement dans mon CSV via LibraryManager."

5. Pourquoi commencer par Steam ?
Fiabilité : L'API appdetails de Steam est la plus documentée et la plus stable du marché.
Qualité : C'est là que tu trouveras les plus belles descriptions et les trailers les plus faciles à télécharger (souvent en MP4 direct sans protection).
Template : Une fois que Steam fonctionne, on pourra copier 80% du code pour GOG et Epic, car la logique de "Récupérer ID -> Chercher Infos -> Télécharger Médias" sera déjà en place.	


Le Plan de Vol pour Gemini (VS Code)
Initialisation : Créer connectors/steam_cloud.py.
Authentification : Utiliser les cookies de session (référence : backend_steam_network.py) pour récupérer la liste des jeux possédés sur le compte.
Boucle de Scrapping :
Prendre la langue de l'utilisateur (via settings.json).
Appeler l'API Steam Store pour chaque jeu.
Télécharger les images et les vidéos MP4.

Finalisation : Appeler LibraryManager.add_game_to_db pour chaque jeu pour garantir que le CSV est à jour et que les fichiers sont bien nommés (via get_safe_filename).