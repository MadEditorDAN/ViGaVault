IDEAS:
-Download background image from gog sb and use it as gamecard backbround

TO DO MAYBE : 
- SQL LITE
- Pour ne pas perdre l'avantage du CSV (la visibilité), tu pourras utiliser un outil gratuit comme "DB Browser for SQLite". C'est un logiciel qui te permet de voir ta base SQL exactement comme un tableau Excel, de modifier les cases à la main et de faire des recherches. Ça t'enlèvera la peur de "ne plus voir tes données".

Bug/Features :

- Game manager, with Batch game edit, Batch game delete. List the games in a table with the following columns release date/name/platfoms/filter category 1/filter category 2, etc.../developper/publisher. On top of each column put listbox with the content of the below column (like excel filter box) and for the name a text box. If you type in it, it search the game starting with what you type and the list dynamically shrink to that selection.
If you select something in the upper listbox it filter the below list to what you selected.

And a custom word list you can use to filter the gamelist on (goodie, dlc,...)

-In settings, put everything that is in the tab "Local copies" in the tab "Local sources" under the current content. and Remove the tab "Local Copies"
-In settings, create a tab Platforms Manager, move the platform manager in that tab, to do so reduce the size of everything in the platform manager to a "normal text size" remove the gamepad icon and next to each platform name put in smaller text copyright owners of each brand. leave a little space between each connect button.

TO DO : 

When adding a platform, first do a full Data Discovery Dump with a separate script to assess the data we can recover from that platform. Get all the metadata, all the image url's and files and all the video url's. Analyse the results and select the data/files to import.